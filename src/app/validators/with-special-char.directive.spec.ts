import { WithSpecialCharDirective } from './with-special-char.directive';

describe('WithSpecialCharDirective', () => {
  it('should create an instance', () => {
    const directive = new WithSpecialCharDirective();
    expect(directive).toBeTruthy();
  });
});
